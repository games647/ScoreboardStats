#These settings aren't required if you disable pvpstats
#Here can you configurate your sql setting if you don't have or want to use a mysql database you can also use a sqlite datbase
SQL-Settings:
  Username: 'bukkit'
  Password: 'walrus'
  Isolation: 'SERIALIZABLE'
  Driver: 'org.sqlite.JDBC'
  # If you use mysql, delete org.sqlite.JDBC, comment out
  #Driver: 'com.mysql.jdbc.Driver'
  Url: 'jdbc:sqlite:{DIR}{NAME}.db'
  # MySQL IP address and port, databasename.
  #Url: 'jdbc:mysql://IP:PORT/databaseName'
  Timeout: 1000
